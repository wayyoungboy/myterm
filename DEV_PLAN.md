# MyTerm 开发计划

> 目标：打造一款专业的 SSH 终端管理器，对标 XTerminal
> 技术栈：Tauri 2 (Rust) + React 19 + TypeScript + Tailwind CSS

## 当前开发范围（2026-06-04）

本轮开发只完成 SSH 对接相关能力：

- SSH 连接管理
- SSH 终端
- SFTP 文件管理
- SSH 会话上的服务器监控

明确不纳入本轮开发：

- 笔记
- AI 助手
- 端口转发
- Telnet
- 快捷命令
- 云同步
- RDP

验证服务器：

```bash
ssh -o BatchMode=yes -o ConnectTimeout=10 -p 17244 wayserver@103.112.184.13 'echo MYTERM_SSH_OK && uname -a && pwd'
```

---

## 第一阶段：SSH 核心功能稳定 ✅

**目标：连接稳定、终端可用**

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 1.1 | SSH 连接建立 | ✅ | TCP + SSH 握手 + 认证 |
| 1.2 | 终端 I/O | ✅ | xterm.js + PTY |
| 1.3 | 会话保持 | ✅ | SshSession 生命周期修复 |
| 1.4 | 非阻塞读取 | ✅ | reader 线程 EOF 检测 |
| 1.5 | 域名 DNS 解析 | ✅ | ToSocketAddrs 替换 parse |
| 1.6 | 多标签独立实例 | ✅ | 每个 tab 独立 session |
| 1.7 | 连接状态显示 | ✅ | StatusBar 事件驱动 |
| 1.8 | 终端窗口大小调整 | ✅ | ResizeObserver + PTY resize |
| 1.9 | Keepalive 心跳 | ⚠️ | 需验证长时间连接稳定性 |

---

## 第二阶段：界面重设计

**目标：干净、专业、参考 XTerminal 设计**

### 2.1 连接中心（表格视图）
- [x] 主区域改为表格布局（非侧边栏）
- [x] 列：选择框、系统图标、延迟、名称、地址、硬件标签、操作
- [x] 延迟检测（TCP ping，显示 ms）
- [x] 硬件信息标签（OS/核数/内存/硬盘，连接后自动获取）
- [ ] 支持多选、批量操作
- [x] 收藏功能（星标）

### 2.2 连接表单（多标签页）
- [x] 左侧标签导航：基本信息 / 连接设置 / 跳板机 / 代理 / 其他
- [x] 基本信息：名称、地址、端口、认证方式、用户名/密码
- [x] 连接设置：超时时间、心跳间隔、终端编码
- [x] 跳板机：ProxyJump 配置（多级）- UI 已有，功能待实现
- [x] 代理：SOCKS5/HTTP 代理设置 - UI 已有，功能待实现
- [x] 其他：初始化命令、初始路径
- [x] 测试连接按钮（实时反馈）

### 2.3 左侧边栏
- [ ] 协议标签切换（SSH / RDP / Telnet，第一阶段只显示 SSH）
- [ ] 搜索框
- [ ] 分组树（可折叠）
- [ ] 快捷操作按钮精简

### 2.4 全局样式
- [ ] 统一黑色主题（参考 XTerminal 暗色）
- [ ] 绿色强调色（连接按钮、状态指示）
- [ ] 字体统一（13px 基准）
- [ ] 圆角、间距统一规范

---

## 第三阶段：监控面板重做

**目标：参考 XTerminal 监控截图，专业级服务器监控**

### 3.1 系统信息
- [x] OS 图标（自动检测 Ubuntu/CentOS/Alpine 等）
- [ ] IP 地址显示
- [x] OS 版本 + 内核版本
- [x] 时区 + 运行时间
- [x] 主机名

### 3.2 CPU 监控
- [x] 彩色方块进度条（绿→黄→红，参考 XTerminal 设计）
- [x] 每核心独立显示
- [x] 总使用率大字显示
- [x] 负载曲线折线图（3 分钟历史）
- [ ] 进程管理：Top CPU 进程列表

### 3.3 内存监控
- [x] 环形图（donut chart）：已使用/缓存/空闲
- [x] 数值显示（GB 单位）
- [x] Swap 使用情况
- [ ] 进程管理：Top 内存进程列表

### 3.4 网络监控
- [x] 实时速度折线图（双线：上行/下行）
- [x] 当前上传/下载速率
- [x] 累计流量
- [ ] 网卡名称显示

### 3.5 硬盘监控
- [x] 分区使用率进度条
- [x] 读写速率（IOPS）
- [ ] 文件系统类型
- [x] 挂载点信息
- [x] 磁盘总览（总量/已用/可用）

### 3.6 显卡监控（NVIDIA）
- [x] GPU 利用率
- [x] 显存使用
- [x] 温度 + 功耗
- [x] GPU 名称

### 3.7 监控脚本优化
- [x] 参考 xterminal_sub 脚本重写监控数据采集
- [ ] CPU：两次快照对比计算真实使用率
- [ ] 网络：带时间戳的速率计算
- [ ] 进程：支持 GNU ps / BusyBox ps / procfs 三后端
- [ ] 数据格式标准化（JSON）

---

## 第四阶段：SFTP 文件管理

**目标：图形化文件浏览器**

- [ ] 双栏布局（本地 / 远程）
- [ ] 文件列表（名称、大小、权限、修改时间）
- [ ] 目录树导航
- [ ] 上传/下载（拖拽支持）
- [ ] 远程文件在线编辑
- [ ] 批量操作（复制、移动、删除）
- [ ] 权限管理（chmod）
- [ ] 文件搜索

---

## 第五阶段：端口转发

**目标：可视化隧道管理**

- [ ] 本地转发（-L）
- [ ] 远程转发（-R）
- [ ] 动态代理 SOCKS5（-D）
- [ ] 转发列表（状态监控）
- [ ] 一键创建/关闭
- [ ] 流量统计

---

## 第六阶段：高级功能

### 6.1 快速命令
- [ ] 命令库管理（CRUD）
- [ ] 动态变量（${host}、${port}、${username}、${date}）
- [ ] 一键执行
- [ ] 快捷键绑定

### 6.2 笔记
- [ ] Markdown 编辑器
- [ ] 按连接/分组关联
- [ ] 实时预览

### 6.3 AI 助手
- [ ] 命令解释
- [ ] 错误诊断
- [ ] 命令生成

### 6.4 连接导入导出
- [ ] JSON 格式导入导出
- [ ] 密码加密处理
- [ ] 批量导入

### 6.5 设置
- [ ] 主题切换（暗色/亮色）
- [ ] 字体设置
- [ ] 终端配色方案
- [ ] 快捷键自定义

---

## 第七阶段：稳定性和优化

- [ ] 连接断线自动重连
- [ ] 会话录制与回放
- [ ] 多跳代理连接（ProxyJump 链）
- [ ] 凭证管理（统一存储、复用）
- [ ] 内存/CPU 性能优化
- [ ] 错误处理和用户提示完善
- [ ] 单元测试覆盖
- [ ] 打包发布（DMG/MSI/DEB）

---

## 技术债务

- [ ] 清理未使用的 Rust 代码（23 个 warning）
- [ ] 前端组件拆分（TerminalView 过大）
- [ ] TypeScript 类型完善
- [ ] CSS 变量统一（减少硬编码颜色）
- [ ] Tauri 权限配置优化

---

## 里程碑

| 阶段 | 预计周期 | 关键交付 |
|------|---------|---------|
| 第一阶段 | ✅ 已完成 | SSH 连接稳定 |
| 第二阶段 | 1-2 周 | 连接中心 + 表单 + 界面重设计 |
| 第三阶段 | 1 周 | 监控面板重做 |
| 第四阶段 | 1-2 周 | SFTP 文件管理 |
| 第五阶段 | 3-5 天 | 端口转发 |
| 第六阶段 | 1-2 周 | 高级功能 |
| 第七阶段 | 持续 | 稳定性 + 优化 |
